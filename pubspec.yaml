name: icon_font_extractor
description: "Extracts ligatures from icon fonts declared in pubspec.yaml and generates typed IconData constants for use with Flutter's Icon widget."
version: 1.1.0
homepage: https://github.com/mintware-de/icon_font_extractor
repository: https://github.com/mintware-de/icon_font_extractor

environment:
  sdk: ^3.11.5

dependencies:
  yaml: ^3.1.2
  path: ^1.9.0
  args: ^2.5.0
  dart_style: ^3.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.30.0

executables:
  icon_font_extractor:

